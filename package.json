{
  "name": "@team-griffin/react-inversify-inject",
  "version": "0.0.1",
  "main": "lib/index.js",
  "jsnext:main": "lib/index.js",
  "repository": "git@github.com:team-griffin/react-inversify-inject.git",
  "author": "Joshua Thornton <joshua.thornton@heg.com>",
  "license": "MIT",
  "scripts": {
    "prepublish": "npm run build",
    "transpile": "babel src -d lib --copy-files",
    "lint": "npm run lint:js && npm run flow",
    "lint:js": "eslint -c ./config/.eslintrc.js 'src/**/*.js'",
    "lint:js:fix": "eslint --fix -c ./config/.eslintrc.js 'src/**/*.js'",
    "flow": "flow check; test $? -eq 0 -o $? -eq 2",
    "build": "npm run transpile",
    "test": "node_modules/.bin/karma start ./config/karma.conf.js",
    "ci": "npm run lint && npm run test && npm run build",
    "postmerge": "yarn"
  },
  "peerDependencies": {
    "react": "^15.4.2"
  },
  "dependencies": {
    "lodash.forown": "^4.4.0",
    "lodash.reduce": "^4.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.1.21",
    "babel-loader": "^6.1.0",
    "babel-preset-es2015": "^6.1.18",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
